title = "HTMLRadar gitleaks config"

[extend]
useDefault = true

[[rules]]
id = "resend-api-key"
description = "Resend API key"
regex = '''re_[A-Za-z0-9_]{20,}'''
keywords = ["re_"]

[[rules]]
id = "supabase-jwt"
description = "Supabase JWT (anon or service-role)"
regex = '''eyJ[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}'''
keywords = ["eyJ"]

[[rules]]
id = "cloudflare-api-token"
description = "Cloudflare API token"
regex = '''(?i)cloudflare[_-]?api[_-]?token["'\s]*[:=]["'\s]*[A-Za-z0-9_-]{40,}'''
keywords = ["cloudflare"]

[allowlist]
description = "Allow placeholders + example files"
paths = [
  '''\.env\.example$''',
  '''docs/''',
]
regexes = [
  '''SUPABASE_ANON_KEY=$''',
  '''eyJ\.\.\.''',
  '''re_xxxxxxxx''',
  # Fake webhook-signature fixture in payments.test.ts — NOT a real secret,
  # used only to exercise the standardwebhooks HMAC round-trip in tests.
  '''whsec_abc123xyz789defghijklmnop''',
  # PostHog PROJECT keys (phc_...) are public write-only tokens by design —
  # PostHog ships them in every customer's client-side HTML. Personal keys
  # (phx_...) are secret and deliberately NOT allowlisted.
  '''phc_[A-Za-z0-9]{20,}''',
  # IndexNow key: public by protocol design (must be served at htmlradar.com/<key>.txt
  # for search engines to verify ownership). Flagged once in commit 66009b0; now read
  # from the environment.
  '''aa73fbe8e79ef39105fe22e99a64e50c''',
]
