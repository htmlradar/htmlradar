{
  "name": "@htmlradar/proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that serves /r/{slug} — lookup, auth, HTML injection.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240314.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0",
    "wrangler": "^3.40.0"
  }
}
